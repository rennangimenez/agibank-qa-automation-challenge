name: Performance Tests

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      test-plan:
        description: 'JMeter test plan to run (without .jmx extension)'
        required: true
        default: 'blazedemo-load-test'
        type: choice
        options:
          - blazedemo-load-test
          - blazedemo-spike-test

jobs:
  performance:
    name: Performance Tests - ${{ github.event.inputs.test-plan || 'blazedemo-load-test' }}
    runs-on: self-hosted
    env:
      TEST_PLAN: ${{ github.event.inputs.test-plan || 'blazedemo-load-test' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync monitoring config
        run: |
          sudo mkdir -p /opt/monitoring
          sudo rsync -avz --delete --exclude 'scripts/' infra/ /opt/monitoring/
          cd /opt/monitoring && sudo docker compose up -d

      - name: Record start time
        run: echo "JOB_START=$(date +%s)" >> $GITHUB_ENV

      - name: Wait for InfluxDB to be ready
        run: |
          echo "Waiting for InfluxDB..."
          for i in $(seq 1 20); do
            if curl -sf http://localhost:8086/ping > /dev/null 2>&1; then
              echo "InfluxDB is healthy after $i attempt(s)"
              curl -sf "http://localhost:8086/query?q=SHOW+DATABASES" | grep -q jmeter && echo "Database 'jmeter' exists" || echo "WARNING: database 'jmeter' not found yet"
              exit 0
            fi
            echo "  attempt $i/20 - waiting..."
            sleep 3
          done
          echo "ERROR: InfluxDB not reachable after 60s"
          exit 1

      - name: Run JMeter Tests
        run: ./mvnw clean verify -pl performance-tests -Djmeter.test=${{ env.TEST_PLAN }}

      - name: Deploy Performance Report
        if: always()
        run: |
          REPORT_DIR=$(find performance-tests/target/jmeter/reports -mindepth 1 -maxdepth 1 -type d | head -1)
          if [ -n "$REPORT_DIR" ]; then
            rsync -avz --delete "$REPORT_DIR/" /srv/apps/agibank-challenge-reports/performance/
          fi

      - name: Push Pipeline Metrics
        if: always()
        run: |
          chmod +x infra/scripts/push-pipeline-metrics.sh
          DURATION=$(( $(date +%s) - $JOB_START ))
          STATUS="${{ job.status }}"
          bash infra/scripts/push-pipeline-metrics.sh \
            performance \
            "$STATUS" \
            "$DURATION" \
            "${{ github.ref_name }}" \
            "${{ github.sha }}" \
            "${{ github.run_id }}"
        continue-on-error: true
